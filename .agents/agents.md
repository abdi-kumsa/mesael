# Antigravity Workspace Guardrails — Mesael Finance

## 🛠️ Global System Mapping
- Load and enforce global instruction playbooks continuously from: `C:/Users/blu/.agents/skills`
- Primary Global Targets: `@design-taste-frontend`, `@deterministic-design`, `@antigravity-design-expert`

## 💻 Technical Infrastructure
- **Tech Stack**: Next.js (App Router), React, modern CSS (CSS Variables, Flexbox, CSS Grid), Next.js API Routes (`/api/...`).
- **Framework Goal**: Full-stack Next.js application combining high-taste UI frontend with real API route handlers to demonstrate complete enterprise capability for landing the project.

## 🎨 Design System & High-Taste UI Guardrails
- **Layout Depth**: Eliminate flat, uninspiring container panels. Incorporate distinct visual layers using fine, semi-transparent borders (`rgba(255,255,255,0.08)`) and soft, multi-layered box-shadows to introduce depth (Z-axis elevation).
- **Typography Hierarchy**: Establish an intentional typographic scale. Ensure crisp line-height proportions (e.g., `line-height: 1.5` for body, `1.2` for headings) and strict font-weight variance to distinguish structural content instantly.
- **Spacing Discipline**: Enforce consistent, spacious element layouts. Never crowd text or buttons against container margins. Standardize padding and margin steps around a strict geometric system (e.g., multiples of 4px or 8px).
- **Color Execution**: Align all generated elements with a premium, sleek financial dashboard look. Maintain clean contrast metrics that strictly pass WCAG accessibility benchmarks.

## 🤖 Interaction & Verification Rules
- Use smooth micro-interactions, explicit transition timings (`transition: all 0.2s ease-in-out`), and intuitive hover/focus indicator states on all buttons, links, and cards.
- **Deterministic Quality Gate**: Before concluding any visual layout generation task, use internal browser validation mechanisms to audit the interface for optical alignment balance, empty geometric gaps, or broken layout constraints across responsive screen breakpoints.
